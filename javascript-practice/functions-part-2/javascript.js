function add7(num) {
    return num + 7;
}

function multiply (num1, num2) {
    return num1 * num2;
}

function capitalize (str) {
    return str[0].toUpperCase();
}

function lastLetter (str) {
    return str.slice(-1);
}
